---
phase: 1
slug: app-shell
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-16
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: App Shell.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — vanilla HTML/CSS/JS |
| Icon library | none — × button rendered as text/unicode (U+00D7), hamburger as CSS bars in Phase 3 |
| Font | Inter, system-ui, -apple-system, BlinkMacSystemFont, sans-serif |

Source: CLAUDE.md (Electron + vanilla HTML/CSS/JS, no build framework), REQUIREMENTS.md DSGN-02

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, close button internal padding |
| sm | 8px | Overlay button row gap, quit button padding |
| md | 16px | Window inner padding (all sides), overlay content padding |
| lg | 24px | Overlay vertical rhythm between heading and body |
| xl | 32px | Not used in Phase 1 |
| 2xl | 48px | Not used in Phase 1 |
| 3xl | 64px | Not used in Phase 1 |

Exceptions: Close (×) button touch target: 28×28px (larger than the xs/sm tokens, intentional for mouse accuracy on frameless chrome). Overlay backdrop covers the entire 220×360px window.

Source: REQUIREMENTS.md SHELL-04 (220×360px window), CONTEXT.md D-03/D-04

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 13px | 400 | 1.4 |
| Heading | 16px | 600 | 1.25 |
| Display | not used in Phase 1 | — | — |

Font stack for all roles: `Inter, system-ui, -apple-system, BlinkMacSystemFont, sans-serif`

Weights declared: 400 (Body and Label), 600 (Heading only). Maximum 2 weights enforced.

Source: CLAUDE.md (Inter/SF Pro-style sans serif), REQUIREMENTS.md DSGN-02. Sizes default — no upstream override.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #f0f0f0 | Window background, overlay background (at full opacity) |
| Secondary (30%) | #1a1a1a | All body text, heading text, label text |
| Accent (10%) | #FF6B35 | × button hover background, primary CTA button ("Quit Glorb") fill |
| Destructive | #FF6B35 | Quit confirmation button — same orange; no separate red needed for v1 |

Accent reserved for: × close button hover state only, and the destructive "Quit Glorb" confirmation button fill. Nothing else in Phase 1 uses accent.

Secondary color note: #1a1a1a (near-black) for text against #f0f0f0 background gives ≥ 12:1 contrast ratio. The "Keep Running" button uses #1a1a1a text on #f0f0f0 background with a 1px #1a1a1a border — no accent.

Source: REQUIREMENTS.md SHELL-04, DSGN-01. CLAUDE.md (#f0f0f0 background, #FF6B35 accent).

---

## Component Inventory

### 1. Close Button (×)

- Position: top-left of the frameless window, 8px from top, 8px from left
- Size: 28×28px touch target; × glyph rendered at 16px font-size using `×` (U+00D7)
- Default state: transparent background, #1a1a1a glyph at 70% opacity
- Hover state: #FF6B35 background (border-radius: 6px), white glyph (#ffffff) at 100% opacity
- CSS transition: `background 150ms ease, color 150ms ease`
- Action: opens quit confirmation overlay (D-03, D-04)

### 2. Quit Confirmation Overlay

- Coverage: full window (220×360px), `position: absolute; inset: 0`
- Background: #f0f0f0 — same as window (replaces/layers over window content)
- Z-index: 100 (above all window content)
- Layout: vertically centered column, 16px horizontal padding
- Content order (top to bottom):
  1. Heading: "Quit Glorb?" — 16px, weight 600, #1a1a1a
  2. Body: "This will close the app, not just hide the window." — 14px, weight 400, #1a1a1a, line-height 1.5, margin-top 8px
  3. Button row: margin-top 24px, two buttons side-by-side with 8px gap
- "Quit Glorb" button: fills #FF6B35, white text (#ffffff), 14px weight 400, border-radius 8px, padding 8px 16px, flex: 1
- "Keep Running" button: transparent background, 1px solid #1a1a1a border, #1a1a1a text, 14px weight 400, same size/padding as quit button, flex: 1
- Quit button hover: #e55a26 (darkened orange, 10% darker)
- Keep Running button hover: #1a1a1a background, #ffffff text
- Both button transitions: `background 150ms ease, color 150ms ease, border-color 150ms ease`
- "Quit Glorb" action: `app.quit()` via IPC
- "Keep Running" action: hides overlay, returns to normal window view

### 3. Main Content Container

- A single `<div id="app">` at full window size
- Background: #f0f0f0
- No visible content in Phase 1 beyond the × button — placeholder only
- Phase 2 will inject the timer UI into this container

### 4. Tray Icon

- Asset: glorb.png scaled to 18×18px using Electron `nativeImage.createFromPath` with `resize({width: 18, height: 18})`
- Template image: `setTemplateImage(true)` — allows macOS to invert for dark/light menu bar
- Fallback: if glorb.png does not render cleanly at 18px, use the text "◉" as a placeholder NativeImage from a 22×22 canvas (can be refined post-Phase 1)
- Click behavior: toggle window show/hide (D-02)

Source: CONTEXT.md D-08 (Claude's discretion on tray icon), REQUIREMENTS.md SHELL-01

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Quit Glorb" |
| Overlay heading | "Quit Glorb?" |
| Overlay body | "This will close the app, not just hide the window." |
| Cancel action label | "Keep Running" |
| Empty state heading | not applicable — Phase 1 has no data state |
| Empty state body | not applicable |
| Error state | not applicable — Phase 1 has no async operations |
| Destructive confirmation | Quit: "This will close the app, not just hide the window." — confirmed by clicking "Quit Glorb" |

Rationale: The overlay body copy explicitly distinguishes quit from hide because CONTEXT.md D-04 calls this out as the critical UX distinction. "Glorb" is included in the button label ("Quit Glorb" not just "Quit") to reinforce the app-level action. "Keep Running" is used instead of "Cancel" to communicate the result (app continues) rather than the gesture (dismissing a dialog).

Source: CONTEXT.md D-04, D-05 (quit confirmation is the only destructive action in Phase 1)

---

## Interaction Contract

| Trigger | Result | Notes |
|---------|--------|-------|
| Click tray icon (window hidden) | Show window below tray icon | D-02, D-07 |
| Click tray icon (window visible) | Hide window | D-02 |
| Click anywhere outside window (blur) | Hide window | D-01 |
| Click × button | Show quit confirmation overlay | D-03, D-04 |
| Click "Quit Glorb" in overlay | Quit app (`app.quit()`) | D-04 |
| Click "Keep Running" in overlay | Hide overlay, resume normal view | D-04 |
| Cmd+Q | Quit app directly, no confirmation | D-05 |
| Window appears | No animation for Phase 1 — instant show | CONTEXT.md discretion |

Window animation decision: instant appear (no fade). Phase 1 scope is structural, not polished. Keep it simple per CONTEXT.md ("keep it simple for Phase 1").

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

No component registry is used. All UI is hand-authored vanilla HTML/CSS.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
