# Glorb

macOS menu bar Pomodoro timer with OLED eye display. Only Timer function at the moment.

## Run

```bash
npm install
npm start
```

Requires Node.js. Arduino firmware lives in `firmware/` (PlatformIO).
